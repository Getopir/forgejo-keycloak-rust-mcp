# Configuration

Configure the daemon with these values:

- Keycloak issuer.
- OIDC discovery URL.
- MCP audience.
- Public MCP resource URL.
- Listen address.

Use environment variables or a deployment secret file outside the repository. Never commit tokens, client secrets, or private keys.
