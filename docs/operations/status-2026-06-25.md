# Status 2026-06-25

## Completed

- Bootstrapped a clean-room Rust workspace.
- Implemented Phase 0 crates:
  - `identity`: Keycloak OIDC discovery, JWKS fetch, JWT issuer and audience validation.
  - `policy`: deterministic operation registry with scope, risk, and approval metadata.
  - `audit`: structured audit event schema and secret-marker regression.
  - `forgejo-mcpd`: health endpoint, OAuth protected-resource metadata, and authenticated `/mcp` probe.
- Added OpenSpec change material and OPIR-O project artifacts.
- Created live OPIR-O project:
  - Project: `Forgejo Keycloak Rust MCP Gateway`
  - Project id: `aca00262-df4b-4755-9f29-afffb0d85b35`
  - URL: `http://192.168.87.56:8080/projects/aca00262-df4b-4755-9f29-afffb0d85b35`
- Verified `cargo test --workspace` passes.
- Created Forgejo fallback repository `lepton/forgejo-keycloak-rust-mcp` and pushed `main`.
- Updated local `origin` to `ssh://git@192.168.87.91:222/lepton/forgejo-keycloak-rust-mcp.git`; `main` tracks `origin/main`.
- Cloned and ran the project on VM166 at `/home/devops/projects/forgejo-keycloak-rust-mcp`; the working tree is synced to `origin/main`.
- Verified VM166 `cargo test --workspace` passes.
- Started `forgejo-mcpd` on VM166:
  - PID file: `/home/devops/.local/state/forgejo-keycloak-rust-mcp/forgejo-mcpd.pid`
  - Bind: `127.0.0.1:7080`
  - Health: `GET /health` returned HTTP `200`
  - Metadata: `GET /.well-known/oauth-protected-resource` returned HTTP `200`
- Verified VM166 Keycloak agent token flow:
  - Token subject was accepted from the `neutrino-agents` realm.
  - `POST /mcp` authenticated `neutrino1-agent`.
  - Gateway returned HTTP `403` with `missing required scope forgejo:repo:read`, which is the expected Phase 0 policy denial for the current token.
- Updated OPIR-O with live evidence inputs:
  - `1743fca6-7908-45ae-a778-6c37aa3a3457`: source and VM166 run evidence.
  - `9626ea64-41c7-48a4-a9b3-e39a2557a1bb`: VM166 Keycloak verification and blockers.
  - `36314047-26e8-4513-ac82-8cf0d9d65e19`: VM171 monitor execution-route blocker.

## Forgejo Repository Status

Current source state:

```text
git ls-remote ssh://git@192.168.87.91:222/lepton/forgejo-keycloak-rust-mcp.git HEAD refs/heads/main
```

Forgejo repository creation status:

- `lepton/forgejo-keycloak-rust-mcp` exists and contains the project source.
- `git ls-remote ssh://git@192.168.87.91:222/lepton/forgejo-keycloak-rust-mcp.git HEAD refs/heads/main` returns matching `HEAD` and `refs/heads/main` values after each push.
- `git push` to `ssh://git@192.168.87.91:222/Rawholding/forgejo-keycloak-rust-mcp.git` failed because organization push-create is disabled.
- `git push` to `ssh://git@192.168.87.91:222/kentthoresen/forgejo-keycloak-rust-mcp.git` failed because user push-create is disabled.
- The available `forgejo.lan` API token returns `403` for both org and user repo creation.
- The VM116 Lepton OpenBao Forgejo handle can create user repositories, but org repository creation returns `403` because the token lacks `write:organization`.
- Path-scoped credential-manager entries for prior Smoothie work return `401` for repo creation.
- Direct SSH into the Forgejo VM as `devops` is not authorized.
- Proxmox cannot run guest commands in VM118 because QEMU guest agent is not configured.

Needed unblocker for Rawholding org placement: create an empty Forgejo repository named `Rawholding/forgejo-keycloak-rust-mcp` through the web UI/admin CLI or provide an API token with `write:organization`, then run:

```powershell
git remote set-url origin ssh://git@192.168.87.91:222/Rawholding/forgejo-keycloak-rust-mcp.git
git push -u origin main
```

## Lab VM Status

Proxmox readback:

- VMID 103 `blankvm` is a stopped template with a Debian netinst ISO still attached.
- VMID 118 `svc-forgejo` is the live Forgejo service VM, not a blank lab.
- VMID 166 `neutrino1` is reachable at `192.168.87.55` and has `basic_agent 1.0.0`.

The isolated blank Forgejo lab VM should be created from a confirmed cloud-init template or a fresh Debian cloud image, not by cloning live VM118.

## Neutrino Monitor Execution Status

Attempted to run existing OPIR-O project `aca00262-df4b-4755-9f29-afffb0d85b35` through VM171 Neutrino Monitor:

- Route: `POST /project-executions`
- Requested execution id: `fkrm-aca00262-vm166-run-20260625`
- Requested worker pool: `neutrino1`
- Result: request timed out at 90 seconds.
- Readback: `/work-orders?project_id=aca00262-df4b-4755-9f29-afffb0d85b35` returned `status: ready` with `work_order_count: 0`.
- Readback: `/project-executions/fkrm-aca00262-vm166-run-20260625` did not return durable execution JSON before timeout.

Treat VM171 existing-project execution as blocked until the monitor route latency/readback issue is repaired. The VM166 daemon run and Keycloak probe are verified independently.
