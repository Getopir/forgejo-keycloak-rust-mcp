# Operations Status - 2026-06-25

## Public Release Preparation

Public release preparation commit: `37ca77285f3966e35a0fc6063f9d699684a9f115`

Internal Forgejo source: pushed to `Rawholding/forgejo-keycloak-rust-mcp` on `main`.

Internal Forgejo wiki: synchronized from `docs/wiki` to the hosted wiki on both `main` and `master`.

Codeberg target: `https://codeberg.org/GetOpir/forgejo-keycloak-rust-mcp`

Codeberg state:

- Repository exists over HTTPS.
- `v0.4.2` release commit is `c8d9cb5ad138de7cdb79c07a6aa13e349f2990d1`.
- Tags `v0.3.15`, `v0.4.0`, `v0.4.1`, and `v0.4.2` are pushed.
- `0.4.1` stronger-copyleft license update is pushed to internal Forgejo and Codeberg.
- `0.4.2` roadmap documentation patch is pushed to internal Forgejo and Codeberg.
- Codeberg hosted wiki is enabled and published from `docs/wiki`.
- Repo-local future commit author is set to `Kent Thoresen <kentthoresen@gmail.com>`.

## Completed

- Added public license files; superseded later by `AGPL-3.0-or-later` for stronger copyleft.
- Added SPDX license identifiers to Rust source and Cargo manifests.
- Added `CONTRIBUTING.md`.
- Added Codeberg publishing instructions in `docs/codeberg-publishing.md`.
- Added source-controlled wiki navigation and `Further-Planned-Improvements.md`.
- Removed an unnecessary string leak in OAuth protected-resource metadata generation.
- Confirmed no live token values or internal host references in public files outside preserved original inputs.

## Verification

- `cargo fmt --check`
- `cargo test --workspace`
- `git diff --check`
- Token/private-key pattern scan: only documentation warnings and audit test marker strings.
- Internal hostname scan outside `docs/original-inputs`: no matches.

## Remaining Blocker

No active publication blocker remains. Existing public source commits before this note are authored as Codex unless history is explicitly rewritten.

## License Update

The project was moved to `AGPL-3.0-or-later` as a stronger copyleft license for network server software. The change is published as `0.4.1`.

## Roadmap Documentation Update

The wiki roadmap was expanded for `0.4.2` with detailed Phase 1, Phase 2, and Phase 3 implementation notes covering principal mapping, trusted-header delegation, read-only metadata, curated tools, bounded output, approval gates, generated endpoint classification, admin separation, and destructive-operation safeguards.

## OPIR-O Loops Update

The OPIR-O project loops page was normalized after publication:

- Project primary workflow now points to the public release workflow.
- Outcome/task backbone is visible in the loops page.
- `FKRM-L001` Phase 0 foundation tasks are marked done.
- `FKRM-L005` lab and Neutrino verification tasks are marked done.
- Public `0.4.2` release evidence tasks are marked done.
- `FKRM-L003` curated MCP and CLI surface tasks are marked done after the
  `v0.10.0` release and live service deployment.
- `FKRM-L004` generated Forgejo API coverage work started in `0.11.0`.
- Latest OPIR-O refresh evidence: `a804d0fe-7d86-41e1-9a94-c5a0d0a16946`.

## Phase 3 Generated Coverage Update

`0.11.0` starts Phase 3 generated coverage:

- pinned the live Forgejo `15.0.3+gitea-1.22.0` Swagger document under
  `vendor/forgejo-api`;
- recorded source provenance and SHA-256
  `a90f2fe1266a7a08dfcf682cd28db96c364e18a7de2a4e559a26afe3485bb26f`;
- classified all 491 pinned Forgejo API operations by target type, risk,
  approval requirement, and exposure;
- added `forgejo_api_coverage` as a bounded metadata-only MCP operation;
- added `forgejo-mcpctl api-coverage`;
- generated `docs/generated/forgejo-api-coverage.md`;
- kept non-reviewed generated endpoints disabled until a semantic overlay,
  scope, risk class, output limit, and approval policy are reviewed.

Verification:

- `py tools/generate_forgejo_api_catalog.py`
- `cargo fmt --check`
- `cargo test --workspace`

Release and deployment readback:

- `main` on Forgejo and Codeberg: `c5c817960f2f94210930098348abf4e6ec09fe4d`.
- Tag `v0.11.0` pushed to Forgejo and Codeberg.
- Hosted Codeberg release:
  `https://codeberg.org/GetOpir/forgejo-keycloak-rust-mcp/releases/tag/v0.11.0`.
- VMID `118` / `svc-forgejo` deployment checkout:
  `c5c817960f2f94210930098348abf4e6ec09fe4d`, exact tag `v0.11.0`.
- `forgejo-keycloak-rust-mcpd.service` restarted and returned `active`.
- `GET http://192.168.87.91:7080/health` returned
  `{"service":"forgejo-mcpd","status":"ok"}`.
- `forgejo-keycloak-rust-mcpctl --help` on the VM lists `api-coverage`.
- Unauthenticated `forgejo_api_coverage` returned `401`, confirming the new
  generated-coverage operation remains behind Keycloak bearer auth.
- OPIR-O `FKRM-L004` tasks were marked done with evidence
  `5f829ad4-b427-43ea-9ad5-e478895868c3`.
- Hosted wiki sync:
  - Forgejo wiki `main`: `6e884d4f81ef0f89864aa2351a3cf5201030a874`.
  - Codeberg wiki `main`: `fa9ed73ff0ae3ca0154eaa43a9ea587062cc50e5`.

## Phase 1 Implementation Update

`0.5.0` implements the first Phase 1 slice:

- explicit Keycloak `(issuer, subject)` to Forgejo principal mapping;
- disabled and unknown principal rejection before Forgejo-backed calls;
- `list_repository_metadata` backed by Forgejo API;
- per-principal Forgejo token environment variable lookup;
- trusted reverse-proxy header derivation from mapped Forgejo principal data;
- OpenSpec and wiki updates for the implemented Phase 1 baseline.

Verification for the Phase 1 implementation:

- `cargo fmt --check`
- `cargo test --workspace`
- `cargo run -p forgejo-mcpd -- --help`
- `git diff --check`
- secret/token scan
- public-doc internal-host scan outside preserved original inputs and operations notes

## Phase 1 Hardening And Phase 2 Baseline Update

`0.6.0` hardens the Phase 1 baseline and starts Phase 2:

- rejects duplicate principal mappings after issuer normalization;
- rejects empty required mapping fields;
- restricts `api_token_env` names to ASCII letters, digits, and underscore;
- rejects caller-supplied trusted identity headers on `/mcp`;
- adds server-capped `limit` and page-token `cursor` handling;
- implements `list_repository_issues`;
- implements `create_issue_comment`;
- implements `list_pull_requests`;
- implements `list_pull_request_reviews`;
- implements `list_releases`;
- implements `list_notifications`;
- returns approval-required responses for high-risk operations without an approval ID.

Remaining after `0.6.0`:

- persistent approval store;
- release creation execution;
- pull-request merge execution;
- admin and destructive execution;
- generated Forgejo API coverage after endpoint classification.

## Phase 2 Resource URI And CLI Update

`0.7.0` completes the remaining Phase 2 repository tasks:

- returns stable `forgejo://...` resource URIs in bounded summaries;
- accepts resource URI targets for repository metadata and numbered issue/pull targets;
- adds the `forgejo-mcpctl` CLI wrapper;
- documents CLI usage and resource URI forms in repo docs and wiki;
- adds `deny.toml`, `docs/security-checks.md`, and optional self-hosted Forgejo Actions workflow for fmt/check/test/audit/deny/SBOM.

Forgejo live readback during this pass:

- `ssh -T forgejo.lan` authenticated as `kentthoresen` with key `id-ed25519-workstation`.
- temporary branch push/read/delete over Forgejo SSH succeeded on `Rawholding/forgejo-keycloak-rust-mcp`.
- `http://192.168.87.91:3000/api/v1/version` returned `15.0.3+gitea-1.22.0`.
- `http://192.168.87.91:3000/explore/repos` returned `200`.

Live Phase 2 MCP testing remains pending because `192.168.87.91:7080` was not listening for `forgejo-mcpd`. REST API auth was not available from the stored Git credentials, and HTTP Git agent push with the stored agent credential hung, so the verified live Forgejo path is SSH Git access.

The hosted Codeberg release object for `v0.7.0` also remains pending. The Codeberg API returned `404` for the release tag before publication, and no Codeberg API token or `tea` CLI was available in the local environment. Release text is prepared in `docs/promotion/codeberg-release-v0.7.0.md`.

## Approval Store, Merge, And Release Execution Update

`0.8.0` added persistent, file-backed approval records for high-risk operations:

- exact operation, target, state, and body-hash binding;
- approver Keycloak subject and mapped Forgejo login recording;
- expiry enforcement;
- validation failures for missing, revoked, expired, mismatched, or wrong-principal approvals.

`0.9.0` made `merge_pull_request` the first executable high-risk operation:

- dry-run merge preview without Forgejo mutation;
- single-use approval consumption before the Forgejo merge API call;
- approver/executor separation by Keycloak subject and mapped Forgejo login;
- CLI support through `forgejo-mcpctl merge-pull-request`.

`0.10.0` extends the same approval execution model to `create_release`:

- dry-run release preview without Forgejo mutation;
- typed release payload fields for `tag_name`, `target_commitish`, `name`, `body`, `draft`, `prerelease`, and `hide_archive_links`;
- single-use approval consumption before the Forgejo release API call;
- CLI support through `forgejo-mcpctl create-release`.

Verification for the latest local pass:

- `cargo fmt --check`
- `cargo check --workspace`
- `cargo test --workspace`
- `cargo audit`

Remaining external blockers:

- Hosted Codeberg release objects still require a Codeberg API token or manual frontend action. Git tags can be pushed without that token, but the Codeberg release page is separate from the tag.
- Live MCP endpoint integration testing on `192.168.87.91` remains blocked until `forgejo-mcpd` is deployed and listening, or shell/deploy access to the test Forgejo VM is available. Current SSH access is valid for Forgejo Git operations, not VM shell commands.

## Live Endpoint And Codeberg Release Unblock

The `v0.10.0` hosted Codeberg release object was created and read back successfully:

- `https://codeberg.org/GetOpir/forgejo-keycloak-rust-mcp/releases/tag/v0.10.0`
- API readback for `/releases/tags/v0.10.0` returned `200`.

The Codeberg API token used for release automation was stored in OpenBao at:

```text
kv/data/prod/codeberg/rawholding/forgejo-keycloak-rust-mcp/release-token
```

The live Rust MCP gateway was deployed to `svc-forgejo` / `192.168.87.91` as a separate service:

- systemd unit: `forgejo-keycloak-rust-mcpd.service`
- binary: `/usr/local/bin/forgejo-keycloak-rust-mcpd`
- config: `/etc/forgejo-keycloak-rust-mcpd/env`
- approval store: `/var/lib/forgejo-keycloak-rust-mcpd/approvals.jsonl`
- endpoint: `http://192.168.87.91:7080/mcp`

Live readback:

- `GET /health` returned `{"service":"forgejo-mcpd","status":"ok"}`.
- `GET /.well-known/oauth-protected-resource` returned resource metadata.
- `POST /mcp` with valid JSON and no bearer token returned `401`, as expected.
- `systemctl is-active forgejo-keycloak-rust-mcpd.service` returned `active`.

## Stable 1.0.0 Release And Live Forgejo Install

`v1.0.0` is the first stable release line for the clean-room Rust gateway.

Git and release state:

- Main commit: `0d12c82939ee10f19d6c66e9184aed7d847bab0b`.
- Annotated tag: `v1.0.0`.
- Forgejo `Rawholding/forgejo-keycloak-rust-mcp` main and `v1.0.0` tag both read back at the expected commit/tag object.
- Codeberg `rawholding/forgejo-keycloak-rust-mcp` main and `v1.0.0` tag both read back at the expected commit/tag object.
- Codeberg release: `https://codeberg.org/GetOpir/forgejo-keycloak-rust-mcp/releases/tag/v1.0.0`.
- Forgejo hosted wiki head: `ea84d651a8ea832093726a96a16e6597928de50e`.
- Codeberg hosted wiki head: `c0203bcce69652557a861095494d7eac849a52aa`.

Hardening and documentation updates:

- README now describes shipped features instead of presenting completed work as roadmap-only phases.
- `docs/features.md` and `docs/wiki/Features.md` document the 1.0.0 feature surface for humans and agents.
- `docs/wiki/Roadmap.md` was reframed as implemented features plus remaining roadmap.
- `forgejo_api_coverage` now has a dedicated `query` field while preserving `target` as a backward-compatible alias.
- OAuth protected-resource metadata now deduplicates repeated operation scopes.

Live install on VMID `118` / `svc-forgejo`:

- Checkout deployed at `0d12c82939ee10f19d6c66e9184aed7d847bab0b`.
- Exact deployed tag: `v1.0.0`.
- Service: `forgejo-keycloak-rust-mcpd.service`.
- Endpoint: `http://192.168.87.91:7080/mcp`.
- Health: `GET /health` returned `{"service":"forgejo-mcpd","status":"ok"}`.
- Unauthenticated `POST /mcp` returned `401`, as expected.
- Protected-resource metadata advertises issuer `http://keycloak:8080/realms/master` and a deduplicated scope list.

Authenticated live MCP readback:

- Keycloak client `forgejo-mcp-live-agent` issues a service-account token with audience `mcp-server` and scope `forgejo:repo:read`.
- The gateway principal map binds the Keycloak service-account subject to Forgejo account `kentthoresen`; the map stores only the token environment-variable name.
- `gateway_probe` for `list_repository_metadata` returned `200`.
- `list_repository_metadata` for `rawholding/forgejo-keycloak-rust-mcp` returned `200`.
- `forgejo_api_coverage` with `query=repository` and `limit=3` returned `200`.
- `list_repository_issues` returned `403` because the test token intentionally lacks `forgejo:issue:read`.

Security cleanup:

- The legacy Go `forgejo-mcp.service` was stopped and disabled because it exposed a Forgejo token in the process command line.
- Port `8090` is no longer listening.
- The Rust gateway remains active on port `7080`.

## v1.1.0 Pull-Request Workflow Deployment - 2026-06-26

`v1.1.0` adds the missing branch-to-pull-request MCP bootstrap and operation
discovery endpoint:

- Source commit: `902fff732708290e8d7e4b65d3a418048ed03746`.
- Commit author: `Kent Thoresen <kentthoresen@users.noreply.codeberg.org>`.
- Forgejo `Rawholding/forgejo-keycloak-rust-mcp` `main` is at the source commit.
- Codeberg `GetOpir/forgejo-keycloak-rust-mcp` `main` is at the source commit.
- Hosted Forgejo wiki `main`: `d17b4c94b21f60997fffcde7d60fddc4b3280598`.
- Hosted Codeberg wiki `main`: `a3f389a4e7bdd697b2a088f07c81887200100e01`.
- Live checkout on `svc-forgejo` / VMID `118`: source commit
  `902fff732708290e8d7e4b65d3a418048ed03746`.
- Service: `forgejo-keycloak-rust-mcpd.service`.
- Endpoint: `http://192.168.87.91:7080/mcp`.

Local verification:

- `cargo test --workspace`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `forgejo-mcpctl create-pull-request --help`
- `forgejo-keycloak-rust-mcpd --help`

Live readback:

- `GET http://192.168.87.91:7080/health` returned
  `{"service":"forgejo-mcpd","status":"ok"}`.
- `GET http://192.168.87.91:7080/capabilities` returned
  `create_pull_request` with scope `forgejo:pr:write`.
- `/capabilities` also lists planned disabled operations:
  `update_pull_request`, `request_reviewers`, `get_branch_status`,
  `get_required_checks`, and `get_pr_checks`.
- Unauthenticated `POST /mcp` returned `401`, preserving the auth boundary.
- Protected-resource metadata now includes `forgejo:pr:write`.

OPIR-O update:

- OPIR-O project `aca00262-df4b-4755-9f29-afffb0d85b35` was updated through
  the M2M API using the VM171 guest-agent token path.
- Evidence input ID:
  `71e8c0a8-7ed8-4fbd-aeeb-75a2839c4302`.
- VM171 current guest-agent IP was `192.168.87.64`, not the stale
  `192.168.87.171` address.

Follow-up live auth readback - 2026-06-26:

- VM166 `neutrino1` successfully authenticated to Keycloak and exchanged its
  agent JWT for an OpenBao token through `auth/keycloak/login`.
- The VM166 OpenBao policy is scoped to `kv/neutrino/agents/neutrino1/*`; it
  denied the production MCP secret paths, as intended for least privilege.
- A VM166-scoped OpenBao handle was provisioned at the agent's own
  `forgejo-keycloak-rust-mcp` handle path with the Keycloak MCP client id,
  client secret, token URL, scopes, and gateway URL. The secret value was not
  printed or written to the repository.
- Keycloak client `forgejo-mcp-live-agent` was confirmed as a confidential
  service-account client in the `master` realm.
- The client was missing the optional `forgejo:pr:write` client scope. The
  scope was added to the live Keycloak client and Keycloak CT `102` was
  restarted from the `services` Proxmox node so runtime cache picked it up.
- Keycloak now issues a short-lived MCP bearer token with issuer
  `http://keycloak:8080/realms/master`, audience `mcp-server`, and scopes
  `forgejo:repo:read` plus `forgejo:pr:write`.
- Authenticated `gateway_probe` for `create_pull_request` returned `200`.
- Authenticated `create_pull_request` dry-run returned `200` with
  `allowed=true` and `approval_required=true`; no Forgejo mutation was made.
- VM166 then read its OpenBao handle, minted a short-lived MCP bearer token,
  and repeated the live `create_pull_request` dry-run successfully through the
  agent path.
- OPIR-O evidence input
  `52893ee3-11c7-4d87-9e14-347a6cc80384` was added to outcome
  `7300459c-0663-4cce-97d4-d667767b3da7` (`FKRM-L003: Curated MCP and CLI
  surface`) with this readback.

Remaining execution blocker:

- The live principal map contains one mapped principal only:
  `forgejo-mcp-live-agent` -> Forgejo account `kentthoresen`.
- Real approval-backed `create_pull_request` mutation requires an approval
  record created by a different mapped principal. Add a second mapped
  human/approver or agent/executor identity before running the mutating PR
  creation path end to end.
- Live Keycloak discovery still advertises issuer
  `http://keycloak:8080/realms/master`. Changing that safely requires Keycloak
  admin or deployment access so the issuer, discovery metadata, MCP metadata,
  OPIR inputs, and agent docs can be moved to one canonical URL together.
