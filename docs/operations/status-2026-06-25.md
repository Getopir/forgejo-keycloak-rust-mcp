# Operations Status - 2026-06-25

## Public Release Preparation

Public release preparation commit: `37ca77285f3966e35a0fc6063f9d699684a9f115`

Internal Forgejo source: pushed to `Rawholding/forgejo-keycloak-rust-mcp` on `main`.

Internal Forgejo wiki: synchronized from `docs/wiki` to the hosted wiki on both `main` and `master`.

Codeberg target: `https://codeberg.org/rawholding/forgejo-keycloak-rust-mcp`

Codeberg state:

- Repository exists over HTTPS.
- `main` is pushed at `c8d9cb5ad138de7cdb79c07a6aa13e349f2990d1`.
- Tags `v0.3.15`, `v0.4.0`, `v0.4.1`, and `v0.4.2` are pushed.
- `0.4.1` stronger-copyleft license update is pushed to internal Forgejo and Codeberg.
- `0.4.2` roadmap documentation patch is pushed to internal Forgejo and Codeberg.
- Codeberg hosted wiki is enabled and published from `docs/wiki`.
- Repo-local future commit author is set to `Kent Thoresen <kentthoresen@gmail.com>`.

## Completed

- Added public license files; superseded later by `AGPL-3.0-or-later` for stronger copyleft.
- Added SPDX license identifiers to Rust source and Cargo manifests.
- Added `CONTRIBUTING.md`.
- Added Codeberg publishing instructions in `docs/codeberg-publishing.md`.
- Added source-controlled wiki navigation and `Further-Planned-Improvements.md`.
- Removed an unnecessary string leak in OAuth protected-resource metadata generation.
- Confirmed no live token values or internal host references in public files outside preserved original inputs.

## Verification

- `cargo fmt --check`
- `cargo test --workspace`
- `git diff --check`
- Token/private-key pattern scan: only documentation warnings and audit test marker strings.
- Internal hostname scan outside `docs/original-inputs`: no matches.

## Remaining Blocker

No active publication blocker remains. Existing public source commits before this note are authored as Codex unless history is explicitly rewritten.

## License Update

The project was moved to `AGPL-3.0-or-later` as a stronger copyleft license for network server software. The change is published as `0.4.1`.

## Roadmap Documentation Update

The wiki roadmap was expanded for `0.4.2` with detailed Phase 1, Phase 2, and Phase 3 implementation notes covering principal mapping, trusted-header delegation, read-only metadata, curated tools, bounded output, approval gates, generated endpoint classification, admin separation, and destructive-operation safeguards.

## OPIR-O Loops Update

The OPIR-O project loops page was normalized after publication:

- Project primary workflow now points to the public release workflow.
- Outcome/task backbone is visible in the loops page.
- `FKRM-L001` Phase 0 foundation tasks are marked done.
- `FKRM-L005` lab and Neutrino verification tasks are marked done.
- Public `0.4.2` release evidence tasks are marked done.
- Phase 1, Phase 2, and Phase 3 implementation tasks remain pending.
